# Base URL of the my-sensein-book backend API.
# Empty / unset  -> use the built-in mock API (Next route handlers under src/app/api/v1).
# Set this to switch the whole app to the Go backend (my-sensein-book-backend):
#   NEXT_PUBLIC_API_BASE=http://localhost:8090
NEXT_PUBLIC_API_BASE=
